#ident	"@(#)set:common/set/network/set.4	1.5"
#ident	"$Header: $"
.optstring
Your options are:
.selection
Press '1' or '2' followed by 'ENTER':
.pageno
Page %d of %d
.ul
Networking Set Installation
.hhelp_ban
Instructions
.helpbanner
Help: Package Dependency Warning
.ur
Package Dependency Warning
#.ll
#Please make a selection.  Press F1 for Help.
.lr
F1=Help
.top
The "`echo $PKG`" package is unable to function correctly unless the package
"`echo $DEPENDENCY`" is also installed.  On the previous screen you did not 
choose to install "`echo $DEPENDENCY`".  If you would like both packages to 
be installed, answer "Yes" to the question below.  Otherwise neither package 
will be installed.
.form
//2 2//YES::Yes//NO::No//Install `echo ${DEPENDENCY}`?//INST_DEP//
//Left/Right arrow keys for next choice (2 choices)//
.button
Apply
Reset
.bottom
Press 'TAB' to move the cursor between fields.  When finished, move the
cursor to "Apply" and then press 'ENTER' to continue.
.help
For detailed instructions, press the 'F1' (or '?') key at any time.

The "`echo $PKG`" package is unable to function correctly unless the
"`echo $DEPENDENCY`" package is also installed.  On the Package Selection
screen you did not choose to install "`echo $DEPENDENCY`".  Since both are
included in this set, you may now choose to install either both or neither
of these packages.
.helpinst
ESC=Exit help  F1=Instructions  1=Forward  2=Backward
.hhelpinst
ESC=Exit instructions  1=Forward  2=Backward
.working
Working...
.end


