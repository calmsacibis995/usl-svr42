Use the Back button to move the window or icon at the pointer location
to the back of the workspace and tuck it behind any overlapping windows
and icons.

Drag MENU onto the button and release MENU to move the window or
icon at the pointer location to the back of the workspace. You can
bring a window or icon to the front of the screen by selecting it.
To select a window, move the pointer to the window background and
click SELECT. To select an icon, move the pointer onto an icon and
click SELECT.
