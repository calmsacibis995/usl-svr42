#!/usr/bin/sh
#ident	"@(#)lp:filter/pcldir/pcl.filter	1.2"
#ident	"$Header: $"

###############
# PCL filter
# converts input-type "simple" to "PCL"
# maps <new-line> into <carriage return><new-line>
###############

sed 's/$//'

